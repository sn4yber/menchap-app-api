# Variables de entorno para la aplicación API JDBC
# Copia este archivo como .env y configura tus valores reales

# Puerto del servidor
PORT=8080

# Configuración de la base de datos
DATABASE_URL=jdbc:postgresql://localhost:5432/api_jdbc
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password

# Configuración CORS - Orígenes permitidos separados por comas
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://tu-dominio.com

# Para producción (Neon), usar algo como:
# DATABASE_URL=jdbc:postgresql://[host]/[database]?sslmode=require&channel_binding=require
# DATABASE_USERNAME=[usuario]
# DATABASE_PASSWORD=[password_seguro]
